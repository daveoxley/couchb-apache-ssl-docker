<Location />
        ProxyPass http://127.0.0.1:5984/ retry=10
        AuthType Basic
        AuthName "Acralyzer"
        AuthUserFile /etc/apache2/passwords
        AuthGroupFile /etc/apache2/groups
        Require group admin
</Location>

